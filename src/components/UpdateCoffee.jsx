import React from 'react';

const UpdateCoffee = () => {
    return (
        <div>
            <h2>This is the section where admin can update the coffee details</h2>
        </div>
    );
};

export default UpdateCoffee;